# セキュリティのため、APIキーなどが載る .env は引き続き除外 
.env

# ログは GitHub に保存する必要がないため除外
logs/

# Python の一時ファイルも除外
__pycache__/
*.pyc

# --- 以下、削除またはコメントアウト ---
# db/       <- 重複排除のために Git 管理が必要
# reports/  <- 成果物として GitHub で閲覧するために Git 管理が必要